{
  "name": "ageofaimpires",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.19.0"
  },
  "scripts": {
    "dev": "tinacms dev -c \"astro dev\"",
    "build": "tinacms build && astro check && astro build",
    "build:local": "tinacms build --local --skip-cloud-checks && astro check && astro build",
    "preview": "astro preview",
    "check": "tinacms build --local --skip-cloud-checks && astro check",
    "social": "node scripts/generate-social-cards.mjs"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.3.2",
    "@astrojs/markdown-remark": "^7.3.1",
    "@astrojs/mdx": "^8.0.1",
    "@astrojs/sitemap": "^3.7.4",
    "@tinacms/astro": "^0.7.0",
    "astro": "^7.3.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@tinacms/cli": "^3.0.0",
    "sharp": "^0.35.4",
    "tinacms": "^3.14.0",
    "typescript": "^5.9.3"
  }
}
